name: safety_assistant
description: AI-Powered Personal Digital Safety Assistant
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.15.0
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^5.0.2
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true